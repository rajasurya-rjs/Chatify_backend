// Arcjet integration removed. This stub exists to prevent runtime import errors if any leftover imports remain.
const aj = {
  protect: async () => ({ isDenied: () => false, results: [] }),
};

export default aj;
